server {
    server_name cortex.dmarantz.com;

    # auth_basic "CORTEX";
    # auth_basic_user_file /etc/nginx/.htpasswd-cortex;
    # BLOCKED: htpasswd file missing — Dan creates it, then uncomment above + remove 503

    location / {
        return 503;
        # root /var/www/cortex;
        # index index.html;
    }

    listen 80;
}
